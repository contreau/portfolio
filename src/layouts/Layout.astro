---
const { description, title, ogURL, pathName, indexIsActive, portfolioIsActive, musingsIsActive } =
  Astro.props;
interface Props {
  description: string;
  title: string;
  ogURL: string;
  pathName: string;
  indexIsActive?: string;
  portfolioIsActive?: string;
  musingsIsActive?: string;
}
const date = new Date().getFullYear();
import GithubSVG from "../svg/GithubSVG.astro";
import LinkedinSVG from "../svg/LinkedinSVG.astro";
import EmailSVG from "../svg/EmailSVG.astro";
import ResumeSVG from "../svg/ResumeSVG.astro";
import Path from "../components/chips/Path.astro";
---

<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="apple-touch-icon" sizes="180x180" href="/apple-touch-icon.png" />
    <link rel="icon" type="image/png" sizes="32x32" href="/favicon-32x32.png" />
    <link rel="icon" type="image/png" sizes="16x16" href="/favicon-16x16.png" />
    <link rel="manifest" href="/site.webmanifest" />
    <meta property="og:type" content="website" />
    <title>Conor Kelley | {title}</title>
    <meta name="title" content={`Conor Kelley | ${title}`} />
    <meta property="og:title" content={`Conor Kelley | ${title}`} />
    <meta name="description" content={description} />
    <meta property="og:description" content={description} />
    <meta property="og:url" content={ogURL} />
    <script>
      const header = document.querySelector("header");
      const stored_classes = sessionStorage.getItem("animatedHeader");
      if (stored_classes === null) {
        sessionStorage.setItem("animatedHeader", "true");
      } else if (stored_classes === "true") {
        header.classList.remove("animated-header");
      }
    </script>
  </head>
  <body>
    <header>
      <div>
        <h1 class="title-name">
          <a href="/"
            ><span style="color: var(--primary);">Conor</span><span
              style="color: var(--light-text);">Kelley</span
            ></a
          >
        </h1>
      </div>
      <nav>
        <ul>
          <li class:list={["", { active: indexIsActive }]}><a href="/">About</a></li>
          <li class:list={["", { active: portfolioIsActive }]}>
            <a href="/portfolio">Portfolio</a>
          </li>
          <li class:list={["", { active: musingsIsActive }]}><a href="/musings">Musings</a></li>
        </ul>
      </nav>
    </header>
    <Path pathName={pathName} />
    <!-- MOBILE NAV -->
    <nav class="mobile-nav">
      <ul>
        <li class:list={["", { active: indexIsActive }]}><a href="/">About</a></li>
        <li class:list={["", { active: portfolioIsActive }]}>
          <a href="/portfolio">Portfolio</a>
        </li>
        <li class:list={["", { active: musingsIsActive }]}><a href="/musings">Musings</a></li>
      </ul>
    </nav>
    <slot />
    <footer>
      <div class="social-links">
        <ul>
          <li>
            <a href="mailto:cfk102@gmail.com" target="_blank" aria-label="Email"><EmailSVG /></a>
          </li>
          <li>
            <a href="https://github.com/contreau" target="_blank" aria-label="Github Profile"
              ><GithubSVG /></a
            >
          </li>
          <li>
            <a
              href="https://www.linkedin.com/in/conor-kelley2/"
              target="_blank"
              aria-label="Linkedin Profile"><LinkedinSVG /></a
            >
          </li>
          <li class="cv">
            <a href="/cv.pdf" target="_blank" aria-label="Curriculum Vitae / Resume"
              ><ResumeSVG /></a
            >
          </li>
        </ul>
      </div>
      <p>&copy; {date} <span style="color: var(--primary)">•</span> Conor Kelley</p>
    </footer>
  </body>
</html>

<style is:global>
  :root {
    --text: #000000;
    --white: #ffffff;
    --light-background: #ffffff;
    --primary: #2059cb;
    --light-text: #090909;
    --dark-text: #ffffff;
  }

  *,
  *::before,
  *::after {
    box-sizing: border-box;
  }

  html {
    font-size: 112.5%; /*18px*/
    scroll-behavior: smooth;
    scrollbar-gutter: stable;
  }

  html,
  body {
    margin: 0;
    padding: 0;
  }

  img {
    max-width: 100%;
    display: block;
    height: auto;
  }

  body {
    font-weight: 400;
    line-height: 1.75;
    background-color: var(--light-background);
    color: var(--light-text);
    font-family: system-ui;
    max-width: 1200px;
    margin-inline: auto;
    display: flex;
    flex-direction: column;
    min-height: 100dvh;
  }

  /* Font Configurations for headings / paragraphs */

  p {
    font-size: clamp(0.85rem, 4vw, 1.05rem);
  }

  h1,
  h2,
  h3 {
    font-family: system-ui;
    line-height: 1.3;
  }

  h1 {
    font-size: clamp(2.5rem, 9vw, 5.5rem);
    font-weight: 500;
    margin-bottom: 0;
    margin-top: 0;

    a {
      text-decoration: none;
    }
  }

  h2 {
    font-size: clamp(1.2rem, 6vw, 1.5rem);
    font-weight: 550;
    color: var(--light-text);
    margin-block: 0;
  }

  h3 {
    font-size: clamp(1rem, 6vw, 1.3rem);
    color: var(--light-text);
    font-weight: 450;
  }

  /* Header / Nav / Footer */

  header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-top: 1.25rem;
    border-bottom: solid 1.5px #1c1c1d7a;
  }

  nav {
    padding-left: 0.2em;
  }

  nav.mobile-nav {
    display: none;
  }

  nav ul {
    display: flex;
    gap: 3rem;
    padding-left: 0;

    li {
      list-style-type: none;
      position: relative;
      font-size: clamp(1rem, 6vw, 1.25rem);
    }

    li.active {
      color: var(--primary);
    }

    li.active::after {
      content: "";
      position: absolute;
      width: 6.5px;
      height: 6.5px;
      background-color: var(--primary);
      border-radius: 50%;
      bottom: -28%;
      transform: translate(-50%, -50%);
      left: 50%;
    }

    li.active a {
      color: var(--primary);
    }

    a {
      text-decoration: none;
      color: var(--text);
      font-weight: 550;
      transition: color 200ms;
    }

    a:hover {
      color: var(--primary);
    }
  }

  section {
    margin-block: 1rem;
    min-height: 340px;
  }

  footer {
    display: flex;
    justify-content: space-between;
    padding-top: 0.5em;
    margin-top: 1rem;

    p {
      margin-block: 0;
      color: #74757b;
      font-weight: 350;
    }
  }

  div.social-links ul {
    display: flex;
    align-items: center;
    gap: 1rem;
    padding-left: 0;
    margin-block: 0;

    li {
      list-style-type: none;
      padding: 0em 0.6em;
      border-radius: 5px;
      padding-inline: 0;
      a {
        text-decoration: none;
        color: #74757b;
        transition: color 200ms;
      }

      a:hover {
        color: var(--primary);
      }
    }
  }

  /* Other global rules */
  p.description {
    margin-block: 1.5rem;
    font-weight: 350;
  }

  /* @ rules */

  @keyframes fadeIn {
    100% {
      opacity: 1;
    }
  }

  @media (max-width: 1100px) {
    nav {
      display: none;
    }
    nav.mobile-nav {
      display: block;
    }

    nav.mobile-nav ul {
      justify-content: center;
      li {
        font-size: clamp(0.85rem, 4vw, 1rem);
      }

      li.active::after {
        width: 5px;
        height: 5px;
      }
    }

    nav ul {
      gap: 1.5rem;
    }

    header {
      margin-inline: auto;
      text-align: center;
      margin-top: 1rem;
    }

    footer {
      align-items: center;

      p {
        min-height: 44px;
      }
    }

    section h2,
    section p.description {
      text-align: center;
    }
  }

  @media (max-width: 500px) {
    footer {
      flex-direction: column;
    }
  }

  @media (max-width: 1200px) {
    body {
      padding-inline: 1em;
    }
  }
</style>
