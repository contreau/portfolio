<header>
  <div class="site-title">
    <h1 class="title-name">
      <a href="/"
        ><span style="color: var(--primary)">Conor</span><span style="color: var(--text);"
          >Kelley</span
        ></a
      >
    </h1>
    <p>
      <span class="gt">&gt;<span class="blink-cursor">_</span></span>
      Web Developer
    </p>
  </div>
</header>

<style>
  header {
    display: flex;
    justify-content: space-between;
    align-items: stretch;
    padding-top: 1.5em;
    padding-bottom: 1.5em;
  }

  .site-title p {
    margin-top: 0.5rem;
    margin-bottom: 0;
    font-weight: 500;
    font-size: clamp(0.8rem, 6vw, 1.2rem);
    color: var(--primary);

    & span {
      color: var(--text);
    }
  }

  span.blink-cursor {
    animation: cursor 1s infinite;
  }

  h1 {
    font-size: clamp(2.5rem, 9vw, 3rem);
    /* font-size: 3.5rem; */
    line-height: 1;
    font-weight: 500;
    margin-bottom: 0;
    margin-top: 0;

    a {
      text-decoration: none;
    }
  }

  /* @ rules */
  @media (max-width: 1100px) {
    header {
      position: relative;
      margin-inline: auto;
      text-align: center;
      flex-direction: column;
      align-items: center;
      gap: 0.75rem;
    }
  }

  @media (min-width: 1200px) {
    header.fixed {
      /* position: fixed; */
      top: 0 rem;
      z-index: 5;
      background-color: var(--bg-color);
      max-width: 1200px;
      width: 100%;
    }
  }

  @keyframes cursor {
    0% {
      opacity: 0;
    }

    20% {
      opacity: 0;
    }

    75% {
      opacity: 1;
    }

    100% {
      opacity: 0;
    }
  }
</style>
